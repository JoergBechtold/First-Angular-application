import { Routes } from '@angular/router';
import { FriutlistComponent } from './friutlist/friutlist.component';

export const routes: Routes = [

        { path: '', component: FriutlistComponent },

];
